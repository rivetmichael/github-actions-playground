on:
  push:
    branches:
      - main

name: test

jobs:
  checks:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Output
        uses: actions/github-script@v6
        # env:
        #   FIRST_NAME: Mona
        #   LAST_NAME: Octocat
        with:
          script: |
            console.log(context)
            console.log(octokit)
