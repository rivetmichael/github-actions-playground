on:
  push:
    branches:
      - main

name: test

env:
  SERVICE_NAME: ${{ vars.SERVICE_NAME || 'default' }}

jobs:
  checks:
    name: Test
    runs-on: ubuntu-latest
    environment:
      name: testing
      url: ''
    steps:
      - uses: actions/checkout@v4
      - run: |
          env

